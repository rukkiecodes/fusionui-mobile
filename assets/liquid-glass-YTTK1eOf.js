import{o as a,a as o,g as i}from"./index-B8N6xyjZ.js";const n={class:"markdown-body"},l={__name:"liquid-glass",setup(s,{expose:t}){return t({frontmatter:{}}),(d,e)=>(a(),o("div",n,[...e[0]||(e[0]=[i(`<h1 id="liquid-glass-on-native" tabindex="-1">Liquid glass on native</h1><p>The signature effect — a refraction slab over a live backdrop. The same <code>engine/liquid-glass</code> core as the web package (SDF → surface normal → Snell refraction) drives the GPU on mobile:</p><ul><li><strong>iOS 26+</strong> → <code>expo-glass-effect</code> — the real <code>UIGlassEffect</code>, system-composited with live backdrop sampling.</li><li><strong>Android / older iOS</strong> → a <code>@shopify/react-native-skia</code> <code>BackdropFilter</code> running <code>GLASS_SKSL</code>, the same refraction math transliterated to SKSL, with chromatic aberration at the rim.</li></ul><pre><code class="language-tsx">import { LiquidGlassView } from &#39;@rukkiecodes/native&#39;
;&lt;LiquidGlassView radius={28} style={{ padding: 20 }}&gt;
  &lt;Text&gt;Frosted, refracted, live.&lt;/Text&gt;
&lt;/LiquidGlassView&gt;
</code></pre><h2 id="the-android-backdrop-limit" tabindex="-1">The Android backdrop limit</h2><p>The Android backdrop-sampling limit is physics, not a bug: an app can’t read pixels behind an arbitrary native view, so the Skia path refracts what’s <em>inside its canvas</em>. Render the backdrop in-canvas, or snapshot it with <code>useBackdropSnapshot</code>.</p><h2 id="same-engine%2C-both-platforms" tabindex="-1">Same engine, both platforms</h2><p><code>GLASS_SKSL</code>, <code>makeGlassUniforms</code>, <code>resolveOptions</code> and <code>DEFAULT_GLASS_OPTIONS</code> are exported from <code>@rukkiecodes/native/engine/liquid-glass</code> — the identical option surface and refraction constants as the web <code>&lt;f-glass&gt;</code>. Tune the bezel, the refraction strength and the aberration once; both platforms honour it.</p><blockquote><p>A live, interactive glass Snack lands here alongside the remaining component pages. For now, the effect ships in the package and runs on device today.</p></blockquote>`,9)])]))}};export{l as default};
